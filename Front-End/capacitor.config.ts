import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.atenatruck.vehicleworkshop',
  appName: 'Vehicle Workshop',
  webDir: 'dist'
};

export default config;
